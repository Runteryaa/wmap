rootProject {
    task dumpMethods {
        doLast {
            try {
                def classLoader = Thread.currentThread().getContextClassLoader()
                def clazz = Class.forName("net.minecraft.client.gui.ActiveTextCollector", false, classLoader)
                println "====== ActiveTextCollector METHODS ======"
                clazz.getMethods().each { method ->
                    println(method.getName() + " : " + java.util.Arrays.toString(method.getParameterTypes()))
                }
            } catch (Exception e) {
                e.printStackTrace()
            }
        }
    }
}
